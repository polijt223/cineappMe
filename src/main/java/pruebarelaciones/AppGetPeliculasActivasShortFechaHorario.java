package pruebarelaciones;
/*
import java.text.SimpleDateFormat;
import java.util.List;

import org.springframework.context.support.ClassPathXmlApplicationContext;

import net.itinajero.app.model.Horario;
import net.itinajero.app.repository.HorarioRepository;
*/
public class AppGetPeliculasActivasShortFechaHorario {

	public static void main(String[] args) {
		/*
		ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext("root-context.xml");
		HorarioRepository repo = context.getBean("horarioRepository",HorarioRepository.class);
		SimpleDateFormat format = new SimpleDateFormat("yyyy-MM-dd");
		List<Horario> lista = null;
		
		try {
			lista = repo.getByFechaPeliculasActivas(format.parse("2019-01-09"));			
			for (Horario horario : lista) {
				System.out.println("Fecha: "+format.format(horario.getFecha())+ " Hora: "+horario.getHora().substring(0, 5)+ " Titulo Pelicula: "+horario.getPelicula().getTitulo());
				}
			
		} catch (Exception e) {
			// TODO: handle exception
		}
	
		
		
		
		context.close();
			*/
	}

}
